-- Rollback Maracatu UUID retrofit P8 / Stage D (mensagens/mensagem_feedback cutover).
--
-- IRREVERSIBLE: the apply migration DROPped the mensagens int id (dropping its SERIAL sequence) and
-- the mensagem_feedback int id (dropping its BIGSERIAL sequence), and DROPped the int
-- mensagem_feedback.mensagem_id FK column, renaming the uuid columns in place over them. The
-- original integer ids and FK values cannot be regenerated by SQL. To recover a committed-but-bad
-- 0030, restore the pre-window backup instead of running this rollback:
--   make restore FILE=<pre-window backup>
-- (the retrofit window verifies `make backup` is restorable before this apply runs).
--
-- This is user data (conversation history + append-only message feedback), so the pre-window backup
-- is the only safe recovery path: there is no derivable mapping back to the dropped int ids.
--
-- No structurally reversible step exists: the child's int mensagem_id FK column and the parents'
-- int ids were dropped, the uuid columns were renamed in place over them, and the
-- mensagem_feedback_mensagem_id_fkey FK (with ON DELETE CASCADE) and both indexes
-- (idx_mensagem_feedback_msg, idx_mensagem_feedback_user_msg) were rebuilt on the uuid columns and
-- cannot be reverted to their int form. This file keeps the yoyo migration pair symmetric and
-- documents the recovery path.

-- mensagens, mensagem_feedback, and the mensagem_feedback.mensagem_id FK column plus the
-- ON DELETE CASCADE FK and the two indexes: NOT reversible here.
-- Recover via `make restore FILE=<pre-window backup>` (see header).
