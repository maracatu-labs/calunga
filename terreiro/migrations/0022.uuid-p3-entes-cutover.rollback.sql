-- Rollback Maracatu UUID retrofit P3 / Stage D (entes cutover).
--
-- IRREVERSIBLE: the apply migration DROPped the entes int id column, which also dropped its
-- SERIAL sequence, and DROPped the int ente_id columns on parlamentares and dados_fiscais.
-- The original integer ids and FK values cannot be regenerated by SQL. To recover a
-- committed-but-bad 0022, restore the pre-window backup instead of running this rollback:
--   make restore FILE=<pre-window backup>
-- (the retrofit window verifies `make backup` is restorable before this apply runs).
--
-- No structurally reversible step exists: the children's int ente_id and the parent's int
-- id were dropped, and the uuid columns were renamed in place over them. This file keeps the
-- yoyo migration pair symmetric and documents the recovery path.

-- entes, parlamentares.ente_id, dados_fiscais.ente_id: NOT reversible here.
-- Recover via `make restore FILE=<pre-window backup>` (see header).
