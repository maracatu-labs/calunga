-- Rollback Maracatu UUID retrofit P5 / Stage D (proposicoes/votacoes/votos/orientacoes cutover).
--
-- IRREVERSIBLE: the apply migration DROPped the proposicoes and votacoes int id columns, which
-- also dropped their SERIAL sequences, and DROPped the int votacoes.proposicao_id,
-- votos.votacao_id, votos.parlamentar_id, and orientacoes.votacao_id columns. The original integer
-- ids and FK values cannot be regenerated by SQL. To recover a committed-but-bad 0026, restore the
-- pre-window backup instead of running this rollback:
--   make restore FILE=<pre-window backup>
-- (the retrofit window verifies `make backup` is restorable before this apply runs).
--
-- No structurally reversible step exists: the children's int FK columns and the parents' int ids
-- were dropped, and the uuid columns were renamed in place over them. The two composite UNIQUE
-- constraints (votos(votacao_id, parlamentar_nome) and orientacoes(votacao_id, partido_bloco)) and
-- the proposicao/votacao FK constraints and indexes were rebuilt on the uuid columns and cannot be
-- reverted to their int form. This file keeps the yoyo migration pair symmetric and documents the
-- recovery path.

-- proposicoes, votacoes, and the votacoes.proposicao_id / votos.votacao_id / votos.parlamentar_id /
-- orientacoes.votacao_id FK columns plus the two composite UNIQUEs: NOT reversible here.
-- Recover via `make restore FILE=<pre-window backup>` (see header).
