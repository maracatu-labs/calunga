-- Rollback Maracatu UUID retrofit P7 / Stage C + D (embeddings/feed_eventos polymorphic
-- backfill and cutover).
--
-- IRREVERSIBLE: the apply migration DROPped the embeddings and feed_eventos int id columns, which
-- also dropped their SERIAL sequences, DROPped the int referencia_id columns on both tables, and
-- DROPped the _uuid_xref snapshot that held the int->uuid mapping for the polymorphic parents. The
-- original integer ids and the int referencia_id values cannot be regenerated by SQL: every
-- polymorphic parent (despesas, contratos, licitacoes, emendas, proposicoes, votacoes, viagens,
-- cpgf, sancoes) already cut over to a UUID PK in P3-P6, so its int ids are gone, and _uuid_xref
-- was the last record of the mapping. To recover a committed-but-bad 0028, restore the pre-window
-- backup instead of running this rollback:
--   make restore FILE=<pre-window backup>
-- (the retrofit window verifies `make backup` is restorable before this apply runs).
--
-- No structurally reversible step exists: the int referencia_id columns and the int ids were
-- dropped and the uuid columns were renamed in place over them, and _uuid_xref was dropped. This
-- file keeps the yoyo migration pair symmetric and documents the recovery path.

-- embeddings (id + referencia_id), feed_eventos (id + referencia_id), _uuid_xref: NOT reversible here.
-- Recover via `make restore FILE=<pre-window backup>` (see header).
