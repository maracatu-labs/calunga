-- Rollback Maracatu UUID retrofit P6 / Stage D (federal leaves cutover).
--
-- IRREVERSIBLE for all six SERIAL leaves (cpgf, despesas_orcamentarias, contratos,
-- licitacoes, viagens, emendas): the apply migration DROPped each int id column, which also
-- dropped its SERIAL sequence. The original integer ids cannot be regenerated by SQL, and
-- the polymorphic embeddings/feed_eventos rows that referenced those ints are resolved in
-- P7 (0028) through the _uuid_xref snapshot, not the int ids. To recover a committed-but-bad
-- 0027, restore the pre-window backup instead of running this rollback:
--   make restore FILE=<pre-window backup>
-- (the retrofit window verifies `make backup` is restorable before this apply runs).
--
-- There is no structurally reversible step: each table's int id was dropped and the uuid
-- column was renamed in place over it. This file keeps the yoyo migration pair symmetric
-- and documents the recovery path.

-- cpgf, despesas_orcamentarias, contratos, licitacoes, viagens, emendas: NOT reversible here.
-- Recover via `make restore FILE=<pre-window backup>` (see header).
