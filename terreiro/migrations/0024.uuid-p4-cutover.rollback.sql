-- Rollback Maracatu UUID retrofit P4 / Stage D (parlamentares/despesas/suspeitas cutover).
--
-- IRREVERSIBLE: the apply migration DROPped the parlamentares and despesas int id columns, which
-- also dropped their SERIAL sequences, and DROPped the int despesas.parlamentar_id and
-- suspeitas.despesa_id columns. The original integer ids and FK values cannot be regenerated by
-- SQL. To recover a committed-but-bad 0024, restore the pre-window backup instead of running this
-- rollback:
--   make restore FILE=<pre-window backup>
-- (the retrofit window verifies `make backup` is restorable before this apply runs).
--
-- No structurally reversible step exists: the children's int FK columns and the parents' int ids
-- were dropped, and the uuid columns were renamed in place over them. The votos->parlamentares FK
-- constraint and idx_votos_parlamentar dropped here are rebuilt by P5 (0026), not restored to int
-- form. This file keeps the yoyo migration pair symmetric and documents the recovery path.

-- parlamentares, despesas, suspeitas.despesa_id, despesas.parlamentar_id: NOT reversible here.
-- votos.parlamentar_id FK + idx_votos_parlamentar: re-established by P5 (0026), not here.
-- Recover via `make restore FILE=<pre-window backup>` (see header).
